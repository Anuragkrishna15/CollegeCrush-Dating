import * as React from 'react';

const BlurredPlaceholder: React.FC = () => {
    return (
        <div className="aspect-square rounded-2xl bg-zinc-800 animate-pulse"></div>
    );
};

export default BlurredPlaceholder;